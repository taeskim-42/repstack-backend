# 프론트엔드 작업 프롬프트: CoreML 기초 체력 측정

## 개요

신규 사용자의 **초기 레벨 판정**을 위한 기초 체력 측정 기능 구현.
CoreML을 활용하여 맨몸운동 횟수를 카운팅하고, 결과를 백엔드에 전송.

---

## 테스트 종류 및 진입점

| 테스트 | 목적 | 방식 | 진입점 |
|--------|------|------|--------|
| **기초 체력 측정** | 최초 레벨 배치 | CoreML 카메라 (푸쉬업/스쿼트/턱걸이) | 온보딩 직후 자동 |
| **승급 테스트** | 레벨업 | 3대 운동 1RM (벤치/스쿼트/데드) | AI 트레이너가 알림 |

---

## 진입 흐름

### 1. 기초 체력 측정 (신규 유저)

```
[회원가입] → [온보딩] → [기초 체력 측정] → [메인 화면]
                              │
                              └── levelAssessedAt == nil 이면 강제 진입
```

### 2. 승급 테스트 (기존 유저)

```
[사용자 운동 기록 축적]
         │
         ▼
[백엔드: 추정 1RM 계산 + eligible_for_test? 체크]
         │ 조건 충족 시
         ▼
┌─────────────────────────────────────┐
│ 🤖 트레이너 (채팅):                  │
│ "운동 기록을 보니 실력이 많이        │
│  늘었네요! 💪 레벨 4 승급 조건을     │
│  충족했어요. 승급 테스트             │
│  도전하시겠어요?"                    │
│                                     │
│  [도전할게요!]  [나중에]             │
└─────────────────────────────────────┘
         │ 도전 선택 시
         ▼
[승급 테스트 화면] → 3대 운동 1RM 입력
```

**승급 알림 조건:**
- 추정 1RM이 다음 레벨 기준 충족
- 마지막 테스트 후 7일 경과
- 최소 운동 횟수 충족 (레벨별 10~30회)

---

## 작업 범위

### 프론트엔드 담당 (iOS)
- [ ] CoreML Pose Detection 구현
- [ ] 운동별 Rep 카운팅 로직
- [ ] 30 BPM 메트로놈/가이드
- [ ] 테스트 UI/UX
- [ ] 결과 API 전송
- [ ] 온보딩 후 자동 진입 로직 (`levelAssessedAt == nil` 체크)
- [ ] 채팅에서 승급 알림 버튼 처리 (Intent: `PROMOTION_ELIGIBLE`)

### 백엔드 담당 (구현 완료 ✅)
- [x] 맨몸운동 결과 수신 API (`submitFitnessTest`)
- [x] 기초 체력 → 초기 레벨 매핑 로직
- [ ] ChatService에서 승급 가능 알림 (작업 중)

---

## 기능 명세

### 1. 테스트 운동 종목

| 운동 | 영문 | 측정 방식 | 비고 |
|------|------|----------|------|
| 푸쉬업 | Push-up | 횟수 카운팅 | 30 BPM 템포 |
| 스쿼트 | Squat | 횟수 카운팅 | 30 BPM 템포 |
| 어시스트 턱걸이 | Assisted Pull-up | 횟수 카운팅 | 밴드/머신 허용 |

### 2. 테스트 규칙

```
- 템포: 30 BPM (1회당 2초)
- 측정 시간: 템포 맞춰서 최대한 많이
- 중단 조건:
  - 사용자가 Stop 버튼
  - 템포 못 맞추면 3회 연속 실패 시 자동 종료
  - 자세 이탈 시 카운트 안 함
```

### 3. CoreML 구현 요구사항

#### Pose Detection
```swift
// Apple Vision Framework 사용
import Vision

// 필요한 관절 포인트
enum RequiredJoints {
    // 푸쉬업: 어깨, 팔꿈치, 손목, 엉덩이
    case shoulder, elbow, wrist, hip

    // 스쿼트: 엉덩이, 무릎, 발목
    case hip, knee, ankle

    // 턱걸이: 어깨, 팔꿈치, 손목
    case shoulder, elbow, wrist
}
```

#### Rep 카운팅 로직 (의사코드)
```swift
// 푸쉬업 예시
class PushupCounter {
    var phase: Phase = .up  // up, down
    var count: Int = 0

    func processFrame(pose: VNHumanBodyPoseObservation) {
        let elbowAngle = calculateAngle(
            shoulder: pose.shoulder,
            elbow: pose.elbow,
            wrist: pose.wrist
        )

        switch phase {
        case .up:
            if elbowAngle < 90 {  // 팔 굽힘
                phase = .down
            }
        case .down:
            if elbowAngle > 160 {  // 팔 펴짐
                phase = .up
                count += 1
                onRepComplete()
            }
        }
    }
}
```

### 4. 30 BPM 가이드

```swift
// 메트로놈 구현
class MetronomeGuide {
    let bpm: Int = 30
    let interval: TimeInterval = 2.0  // 60/30 = 2초

    var timer: Timer?
    var beatCount: Int = 0

    func start() {
        timer = Timer.scheduledTimer(withTimeInterval: interval, repeats: true) { _ in
            self.beatCount += 1
            self.playBeatSound()
            self.updateUI()  // 시각적 가이드
        }
    }
}
```

### 5. UI/UX 요구사항

#### 테스트 진입 화면
```
┌─────────────────────────────┐
│     기초 체력 측정           │
│                             │
│  3가지 운동으로 기초 체력을    │
│  측정합니다.                 │
│                             │
│  ┌─────┐ ┌─────┐ ┌─────┐   │
│  │푸쉬업│ │스쿼트│ │턱걸이│   │
│  │ ⬜  │ │ ⬜  │ │ ⬜  │   │
│  └─────┘ └─────┘ └─────┘   │
│                             │
│      [ 테스트 시작 ]         │
└─────────────────────────────┘
```

#### 운동 측정 화면
```
┌─────────────────────────────┐
│  푸쉬업 테스트               │
├─────────────────────────────┤
│                             │
│     ┌─────────────┐         │
│     │             │         │
│     │  카메라 뷰   │         │
│     │  (Pose 오버레이)      │
│     │             │         │
│     └─────────────┘         │
│                             │
│    🔊 BPM 가이드: ● ○ ○ ○   │
│                             │
│         [ 42 회 ]           │
│                             │
│      [ 측정 완료 ]           │
└─────────────────────────────┘
```

#### 결과 화면
```
┌─────────────────────────────┐
│     측정 완료!               │
│                             │
│  푸쉬업      42회  ✅        │
│  스쿼트      35회  ✅        │
│  턱걸이      12회  ✅        │
│                             │
│  ─────────────────────────  │
│  기초 체력 점수: 78점        │
│  예상 레벨: 중급 (Lv.3)      │
│                             │
│      [ 결과 제출 ]           │
└─────────────────────────────┘
```

---

## API 연동

### 결과 제출 API (구현 완료 ✅)

```graphql
mutation SubmitFitnessTest(
  $pushupCount: Int!
  $squatCount: Int!
  $pullupCount: Int!
  $testDurationSeconds: Int
  $deviceModel: String
) {
  submitFitnessTest(input: {
    pushupCount: $pushupCount
    squatCount: $squatCount
    pullupCount: $pullupCount
    testDurationSeconds: $testDurationSeconds
    deviceModel: $deviceModel
  }) {
    result {
      success
      fitnessScore        # 0-100 점수
      assignedLevel       # 1-8 숫자 레벨
      assignedTier        # beginner/intermediate/advanced
      message             # 피드백 메시지
      recommendations     # 훈련 권장사항 배열
      exerciseResults {
        pushup { count tier tierKorean points }
        squat { count tier tierKorean points }
        pullup { count tier tierKorean points }
      }
      errors
    }
    errors
  }
}
```

### 현재 사용 가능한 관련 API

```graphql
# 사용자 프로필 조회 (레벨 확인)
query Me {
  me {
    id
    userProfile {
      currentLevel      # beginner/intermediate/advanced
      numericLevel      # 1-8
      levelAssessedAt   # null이면 미측정
    }
  }
}

# 개발용 신규 유저 생성 (테스트용)
mutation DevSignInFresh($email: String, $name: String) {
  devSignInFresh(email: $email, name: $name) {
    authPayload {
      token
      user { id name }
    }
    errors
  }
}
```

---

## 데이터 흐름

```
[사용자]
    │
    ▼
┌─────────────────┐
│ 1. 테스트 시작   │
│    카메라 권한   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 2. 푸쉬업 측정   │◀──┐
│    CoreML 카운팅 │   │
└────────┬────────┘   │
         │            │
         ▼            │ 3종목
┌─────────────────┐   │ 순차 진행
│ 3. 스쿼트 측정   │   │
│    CoreML 카운팅 │   │
└────────┬────────┘   │
         │            │
         ▼            │
┌─────────────────┐   │
│ 4. 턱걸이 측정   │───┘
│    CoreML 카운팅 │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 5. 결과 요약     │
│    로컬 저장     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│ 6. API 전송     │────▶│ Backend         │
│ submitFitnessTest│     │ 레벨 계산/저장   │
└────────┬────────┘     └────────┬────────┘
         │                       │
         ▼                       ▼
┌─────────────────┐     ┌─────────────────┐
│ 7. 결과 표시     │◀────│ 레벨 응답       │
│    레벨 안내     │     │ Lv.3 중급       │
└─────────────────┘     └─────────────────┘
```

---

## 기술 스택 권장

| 항목 | 권장 기술 |
|------|----------|
| Pose Detection | Vision Framework (VNDetectHumanBodyPoseRequest) |
| 카메라 | AVFoundation |
| 타이머/메트로놈 | Combine / Timer |
| 상태 관리 | SwiftUI @State / ObservableObject |
| 네트워크 | Apollo iOS (GraphQL) |

---

## 체크리스트

### Phase 1: 기본 구현
- [ ] 카메라 권한 요청 및 프리뷰
- [ ] Vision Framework 연동
- [ ] 기본 Pose 오버레이 표시

### Phase 2: 카운팅 로직
- [ ] 푸쉬업 카운터 구현
- [ ] 스쿼트 카운터 구현
- [ ] 턱걸이 카운터 구현
- [ ] 30 BPM 메트로놈

### Phase 3: UI/UX
- [ ] 테스트 진입 화면
- [ ] 운동별 측정 화면
- [ ] 결과 요약 화면
- [ ] 진행 상태 인디케이터

### Phase 4: API 연동
- [ ] submitFitnessTest mutation 호출
- [ ] 에러 핸들링
- [ ] 오프라인 대응 (로컬 저장 후 재전송)

---

## 참고 자료

- [Apple Vision Framework - Body Pose](https://developer.apple.com/documentation/vision/detecting_human_body_poses_in_images)
- [AVFoundation Camera](https://developer.apple.com/documentation/avfoundation/cameras_and_media_capture)
- [RepStack GraphQL Schema](/docs/api/)

---

## 질문/협의 필요 사항

1. **턱걸이 측정 방식**: 어시스트 턱걸이 시 카메라 각도/위치는?
2. **실패 허용 범위**: 템포 못 맞출 때 바로 종료? 경고 후 계속?
3. **재측정 허용**: 결과 불만족 시 재측정 가능?
4. **오프라인 모드**: 네트워크 없을 때 로컬 저장 후 나중에 전송?

---

*Backend API (`submitFitnessTest`)는 별도 작업 예정*
